spring.datasource.url=jdbc:postgresql://localhost:5433/afriyan1
spring.datasource.username=toto
spring.datasource.password=12345

#spring.datasource.url=jdbc:postgresql://pgbounceryouthfp:6432/${DB_NAME}
#spring.datasource.username=${POSTGRES_USER}
#spring.datasource.password=${POSTGRES_PASSWORD}

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
#validate
springdoc.swagger-ui.path=/swagger-ui-custom.html
spring.web.resources.static-locations=classpath:/static/,file:uploads/
file.upload-dir=uploads/

lmt.accountSid=${LMTACCOUNTSID:asd}
lmt.authToken=${LMTAUTHTOKEN:asd}
lmt.sender=${LMTSENDER:asdad}
lmt.url=${LMT_URL:dasd}
##################################################################################

spring.mail.host=smtp.camoo.com
spring.mail.port=587
spring.mail.username=youthfp@youthfp.cm
spring.mail.password=@Lingot75
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.servlet.multipart.max-file-size=2000MB
spring.servlet.multipart.max-request-size=2000MB

unfpaphoto=classpath:/images/unfpa.png
young=classpath:/images/logo192.png
#spring.main.allow-circular-references=true
weather.website=https://home.openweathermap.org/
weather.key=e6c341471b301f124f2a007bd14d728a
weather.url=https://api.openweathermap.org/data/2.5/forecast?q={ville},CM&appid=${weather.key}&units=metric&lang=fr
server.port=8087

spring.jmx.enabled=false

# Keycloak Configuration (Port 8087 local)


keycloak.url=${KEYCLOAK_URL:http://localhost:8070}
keycloak.realm=${KEYCLOAK_REALM:youthfp}
keycloak.clientid=${KEYCLOAK_CLIENT_ID:youthfp-backend}
keycloak.clientsecret=${KEYCLOAK_CLIENT_SECRET:aah2LFsZJCSHugfCp2ARjB6prWsRqEr3}
keycloak.username=${KEYCLOAK_USERNAME:admin}
keycloak.password=${KEYCLOAK_PASSWORD:admin123}
keycloak.enabled=true
keycloak.uselegacyjwt=true

# MinIO Configuration (API on port 7004 -> 9000, Console on 7003)
minio.endpoint=${MINIO_URL:http://localhost:7004}
minio.fileurl=${MINIO_FILE_URL:http://localhost:7004}
minio.accessKey=${MINIO_ACCESS_KEY:minioadmin}
minio.secretKey=${MINIO_SECRET_KEY:minioadmin123}
minio.bucketName=youthfpstorage
minio.region=us-east-1
minio.enabled=true

# Redis Configuration
spring.data.redis.host=${REDIS_URL:localhost}
spring.data.redis.port=${REDIS_PORD:6379}
spring.cache.type=redis

# Compression
server.compression.enabled=true
server.compression.mime-types=application/json,text/html,text/xml,text/plain,application/javascript,text/css
server.compression.min-response-size=2048

#spring.security.oauth2.client.provider.keycloak.issuer-uri=http://localhost:8070/realms/youthfp
#spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8070/realms/youthfp
spring.security.oauth2.resourceserver.jwt.issuer-uri=${keycloak.url}/realms/${keycloak.realm}
